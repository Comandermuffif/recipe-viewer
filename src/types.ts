export default interface RecipeData {
    title?: string,
    image?: string,
    servings?: number,
    duration?: number,
    ingredients?: string[],
    instructions?: string[],
    notes?: string,
};