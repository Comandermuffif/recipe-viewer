export default interface RecipeData {
    title?: string,
    image?: string,
    servings?: string,
    duration?: string,
    ingredients?: string[],
    instructions?: string[],
    notes?: string[],
};